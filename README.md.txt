# Finance App (IFRS Projections)

A Streamlit app that generates Income Statement, Balance Sheet, and Cash Flow
based on assumptions, with export to Excel and Word. Supports client logins.

## Run locally
```bash
py -m pip install -r requirements.txt
py -m streamlit run finance_app.py
